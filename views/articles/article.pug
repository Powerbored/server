extend /layout

block content
	h1= article.title
	h2 Written by #{author}
	p= article.body
	hr
	if user
		if user.id == article.author
			a(href='/articles/edit/'+article._id) Edit
			button#deleteArticle(data-id=article._id) Delete
			script(src='/js/article-author-control-functions.js')